using UnityEngine;

namespace GGGameOver.Toilet.Game
{
    public class CharacterView : MonoBehaviour
    {
        public void Init(CharacterEntity entity, uint id)
        {
            // Initialize the character view with the given ID and entity
            // This could include setting up animations, UI elements, etc.
        }
    }
}
